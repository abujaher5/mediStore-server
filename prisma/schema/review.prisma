model Review{id          String   @id @default(uuid())
rating      Int
comment     String
userId      String
medicineId  String
medicine    Medicine @relation(fields: [medicineId], references: [id])
createdAt   DateTime @default(now())
updatedAt   DateTime @updatedAt

@@unique([userId, medicineId])
@@index([medicineId])
}